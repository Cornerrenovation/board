# Corner — Board

Weekly KPI scorecard for the Monday leadership call. The page is AES-256-GCM
encrypted (PBKDF2-SHA256, 600k iterations) and decrypted in the browser; this
repo holds ciphertext only. The password is shared separately and never
committed. `noindex` + `robots.txt` keep it out of search.
